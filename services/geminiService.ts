
// Gemini API features are currently disabled.
// No API Key required.

export const getAiTutorResponse = async (userPrompt: string, history: any[]) => {
  return "AI features are currently unavailable.";
};
